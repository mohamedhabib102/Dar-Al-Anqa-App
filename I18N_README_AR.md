# ميزة تعدد اللغات في الموقع 🌍

## نظرة عامة
تم إضافة ميزة تعدد اللغات (Internationalization - i18n) إلى الموقع باستخدام مكتبة `next-intl`. الموقع الآن يدعم لغتين:
- 🇸🇦 **العربية** (ar)
- 🇺🇸 **الإنجليزية** (en)

## المميزات الرئيسية

### 1. تبديل اللغة التلقائي
- زر أنيق لتبديل اللغة في أعلى الصفحة
- قائمة منسدلة تعرض جميع اللغات المتاحة
- تغيير فوري للغة دون إعادة تحميل الصفحة

### 2. دعم RTL/LTR
- العربية: من اليمين إلى اليسار (RTL)
- الإنجليزية: من اليسار إلى اليمين (LTR)
- التبديل التلقائي بين الاتجاهين

### 3. روابط متعددة اللغات
- كل صفحة لها رابط فريد لكل لغة
- مثال:
  - `/en` - الصفحة الرئيسية بالإنجليزية
  - `/ar` - الصفحة الرئيسية بالعربية
  - `/en/books` - صفحة الكتب بالإنجليزية
  - `/ar/books` - صفحة الكتب بالعربية

## البنية التقنية

### الملفات الرئيسية

#### 1. ملفات الترجمة (`messages/`)
```
messages/
├── ar.json  (الترجمات العربية)
└── en.json  (الترجمات الإنجليزية)
```

#### 2. ملفات الإعداد (`i18n/`)
```
i18n/
├── request.ts   (إعدادات الطلبات)
└── routing.ts   (إعدادات التوجيه)
```

#### 3. Middleware
- `middleware.ts` - يتعامل مع توجيه اللغات تلقائياً

#### 4. المكونات
- `components/LanguageSwitcher.tsx` - مكون تبديل اللغة

## كيفية الاستخدام

### في Server Components
```typescript
import { useTranslations } from 'next-intl';

export default function MyPage() {
  const t = useTranslations('HomePage');
  
  return (
    <div>
      <h1>{t('title')}</h1>
      <p>{t('about')}</p>
    </div>
  );
}
```

### في Client Components
```typescript
'use client';

import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations('Navigation');
  
  return (
    <nav>
      <a>{t('home')}</a>
      <a>{t('books')}</a>
    </nav>
  );
}
```

### استخدام الروابط
```typescript
import { Link } from '@/i18n/routing';

// سيتم إضافة بادئة اللغة تلقائياً
<Link href="/books">الكتب</Link>
```

## إضافة لغة جديدة

راجع ملف `HOW_TO_ADD_LANGUAGES.md` للحصول على دليل مفصل خطوة بخطوة.

### الخطوات السريعة:
1. أنشئ ملف JSON جديد في `messages/` (مثل `messages/fr.json`)
2. أضف كود اللغة إلى `i18n/routing.ts`
3. حدّث `middleware.ts`
4. أضف اسم اللغة في ملفات الترجمة الأخرى
5. أعد تشغيل الخادم

## أمثلة على الترجمات

### ملف `messages/ar.json`
```json
{
  "HomePage": {
    "title": "مرحباً بكم في مكتبتنا",
    "about": "من نحن"
  },
  "Navigation": {
    "home": "الرئيسية",
    "books": "الكتب",
    "signIn": "تسجيل الدخول"
  }
}
```

### ملف `messages/en.json`
```json
{
  "HomePage": {
    "title": "Welcome to our Library",
    "about": "About Us"
  },
  "Navigation": {
    "home": "Home",
    "books": "Books",
    "signIn": "Sign In"
  }
}
```

## الحصول على اللغة الحالية

```typescript
import { useLocale } from 'next-intl';

export default function MyComponent() {
  const locale = useLocale(); // 'ar' أو 'en'
  
  return <div>Current language: {locale}</div>;
}
```

## التوجيه البرمجي

```typescript
import { useRouter } from '@/i18n/routing';

export default function MyComponent() {
  const router = useRouter();
  
  const goToBooks = () => {
    router.push('/books'); // سيحافظ على اللغة الحالية
  };
  
  return <button onClick={goToBooks}>Go to Books</button>;
}
```

## الفوائد

✅ **تجربة مستخدم محسّنة**: المستخدمون يمكنهم قراءة المحتوى بلغتهم المفضلة  
✅ **SEO محسّن**: كل لغة لها روابط فريدة قابلة للفهرسة  
✅ **سهولة الصيانة**: الترجمات منفصلة في ملفات JSON  
✅ **قابلية التوسع**: إضافة لغات جديدة سهلة وسريعة  
✅ **أداء عالي**: التبديل بين اللغات فوري بدون إعادة تحميل  

## المكتبات المستخدمة

- **next-intl**: مكتبة i18n قوية لـ Next.js
- **React Icons**: للأيقونات (FaGlobe, FaChevronDown)

## الدعم الفني

إذا واجهت أي مشاكل أو لديك أسئلة، راجع:
- [توثيق next-intl](https://next-intl-docs.vercel.app/)
- ملف `HOW_TO_ADD_LANGUAGES.md` في المشروع
